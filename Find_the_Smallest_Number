let numbers = [25, 10, 45, 3, 18, 7];
function small(numbers){
  let smallest=numbers[0];
  for(let i=0;i<numbers.length;i++){
    if(numbers[i]<smallest){
      smallest=numbers[i];
    }
  }
  return smallest++;
}
console.log(small(numbers));
