let numbers = [-5, 10, -2, 20, 0, 15, -8];
function countPositive(numbers){
  let positive=0;
  for(let i=0;i<numbers.length;i++){
    if(numbers[i]>0){
      positive++;
    }
  }
  return positive;
}
console.log(countPositive(numbers));
